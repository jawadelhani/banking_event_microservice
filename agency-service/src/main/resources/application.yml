server:
  port: 8083

spring:
  application:
    name: agency-service
  datasource:
    url: jdbc:postgresql://localhost:5432/agency
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
